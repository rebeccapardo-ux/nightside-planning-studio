# Accessibility audit — Pass 1 (automated)

**Target standard:** WCAG 2.1 AA, with 2.2 AA considerations where reasonable.
**Date started:** 2026-05-25.
**Status:** Initial findings — runtime axe/Lighthouse runs pending decisions on infrastructure and brand-color cases.

---

## Phase 1 — Tooling

| Tool | Status | Notes |
|---|---|---|
| `eslint-plugin-jsx-a11y` (recommended) | ✅ installed + wired | Surfaces source-level JSX violations on every lint. `eslint-config-next` registers the plugin but only turns on a small subset of its rules; we now run the full `recommended` set. |
| `@axe-core/cli` | ⏸️ deferred | Needs a runner that can hit authed routes. Recommended approach noted in §"Open questions" below. |
| Lighthouse | ⏸️ deferred | Same auth constraint. Chrome DevTools Lighthouse is sufficient for manual spot-checks per route; CLI integration is a separate effort. |

---

## Phase 2 — Initial source-level findings (eslint-plugin-jsx-a11y)

Running `npx eslint 'app/**/*.{tsx,jsx}'` after enabling `recommended` produced **87 jsx-a11y findings** across **12 files**.

### Findings by rule

| Rule | Count | Severity | WCAG criteria | Summary |
|---|---|---|---|---|
| `jsx-a11y/click-events-have-key-events` | 27 | Serious | 2.1.1 Keyboard | Visible elements with `onClick` but no keyboard handler. Most are `<div>`/`<span>` acting as buttons. |
| `jsx-a11y/no-static-element-interactions` | 26 | Serious | 4.1.2 Name, Role, Value | Same set as above — `<div>` with interaction handlers should be `<button>` or have a `role`. |
| `jsx-a11y/label-has-associated-control` | 19 | Serious | 1.3.1 Info and Relationships; 3.3.2 Labels or Instructions | Form labels not programmatically associated with their inputs (e.g. missing `htmlFor`). |
| `jsx-a11y/no-autofocus` | 5 | Moderate | 2.4.3 Focus Order | `autoFocus` is an a11y anti-pattern — moves focus unexpectedly for screen reader and keyboard users. |
| `jsx-a11y/no-noninteractive-element-interactions` | 4 | Serious | 4.1.2 | Click handlers on `<li>`/`<p>`/etc. |
| `jsx-a11y/media-has-caption` | 3 | Moderate | 1.2.2 Captions (Prerecorded) | `<audio>` elements in voice-note components need a `<track>` for captions. **Decision needed** — see §Decisions. |
| `jsx-a11y/alt-text` | 3 | Serious | 1.1.1 Non-text Content | `<img>` missing `alt` attribute. |

### Files affected (jsx-a11y errors only)

```
app/app/capture/page.tsx
app/app/entries/[id]/FinancialInformationSnapshot.tsx
app/app/plan/export/PlanPDFDocument.tsx
app/auth/forgot-password/page.tsx
app/auth/signup/page.tsx
app/components/DomainAssigner.tsx
app/components/FeedbackModal.tsx
app/components/PlanNotesGrid.tsx
app/components/WelcomeModal.tsx
app/components/notes/NoteCard.tsx
app/components/notes/VoiceNoteCard.tsx
app/containers/[id]/page.tsx
```

---

## Phase 3 — Code-level audit of items lint can't see

### Color contrast (brand palette)

Computed against the cream `Light #F8F4EB`:

| Foreground | Hex | Ratio vs Light | Normal text (4.5:1) | Large text / UI (3:1) |
|---|---|---:|:---:|:---:|
| Midnight | `#130426` | 15.2 : 1 | ✅ | ✅ |
| Night | `#2C3777` | 10.2 : 1 | ✅ | ✅ |
| Sunset | `#DB5835` | **4.00 : 1** | ❌ | ✅ |
| Sunrise | `#F29836` | **2.16 : 1** | ❌ | ❌ |
| Dusk | `#BBABF4` | **1.69 : 1** | ❌ | ❌ |

`Light on dark` (cream text on saturated bg):

| Background | vs Light text |
|---|---:|
| Midnight `#130426` | 15.2 : 1 ✅ |
| Night `#2C3777` | 10.2 : 1 ✅ |
| Sunset `#DB5835` | 4.00 : 1 — borderline (passes large text only) |
| Sunrise `#F29836` | 2.16 : 1 ❌ |
| Dusk `#BBABF4` | 1.69 : 1 ❌ |

**Implication:** Dusk + Sunrise can't carry text against the cream background (or vice versa). Sunset is on the line for normal text — passes for headings ≥18pt or ≥14pt bold. There are **230 occurrences** of these three hex values in the codebase; the next pass will need to enumerate which carry text and which are decorative/large-headings before fixing. **Decisions needed** — see §Decisions.

### Focus states

`grep` against `globals.css` and the component tree shows **no `:focus-visible` styles defined anywhere**. The platform relies on browser default focus rings, which:
- vary by browser
- are easy to lose against brand backgrounds (the default ring is light blue and disappears on Dusk)
- aren't visible at all on some custom `<button>` styles that override `outline`

**Decision needed** — focus-style option to pick (§Decisions).

### Reduced motion

Already respected in:
- `app/globals.css` (Legacy Map tooltip animation)
- `app/app/reflect/legacy-map/page.tsx` (tooltip card animation)
- `app/app/reflect/values-ranking/page.tsx`, `fears-ranking/page.tsx` (saved animation)
- `app/components/AppFooter.tsx` (back-to-top smooth scroll)
- `app/components/DomainTour.tsx`, `lib/tour-scroll.ts` (tour scroll + arrow draw)

Animations **not yet checked** for reduced-motion compliance:
- DomainStateCard mini segment bar (no animation? to verify)
- Card-flip animation on `/app/learn/trivia`
- Scenario navigator transitions
- Reflection prompt card transitions
- Slide-panel slide-in animation in `SlidePanel`

Logged as moderate; will sweep these in a dedicated commit.

### Landmarks

Quick read of `LayoutShell`, page templates, and `GlobalNav`:
- `<nav>` element used in GlobalNav and AppFooter — good
- `<main>` element wraps page content in LayoutShell — good
- `<footer>` element used by AppFooter — good
- Some pages render their own `<header>` band but use `<div>`, not `<header>` — moderate
- Breadcrumbs use `<nav aria-label="Breadcrumb">` — good

Recommend a small commit converting per-page header bands to `<header>` elements.

### Heading hierarchy

Not exhaustively checked yet. Spot checks:
- Most pages have a single `<h1>` ✅
- Some pages skip from `h1` → `h3` (no `h2`); needs systematic pass
- Trivia card and tour modals use `<h2>` / `<h3>` inside conditional render — may produce DOM-order skips depending on viewport

This needs a route-by-route check, easiest with axe-core runtime (deferred).

### Touch target size (WCAG 2.2)

Several known small touch targets:
- Mobile drawer hamburger lines (decorative, button is 44×44 — OK)
- Footer-link items on mobile — 40px min-height per recent tour spec; **slightly under** the WCAG 2.2 44×44 recommendation
- "Skip tour" link inside tour modals — small text link; uncertain
- Domain card buttons — adequate; verify

Worth a dedicated mobile-only sweep.

---

## Decisions needed before further fixing

The brief explicitly asks me to surface these.

### A) Focus visible style (pick one)

Option 1 — **Outline-based**:
```css
:focus-visible {
  outline: 2px solid #2C3777;   /* Night */
  outline-offset: 2px;
  border-radius: inherit;
}
```
Pros: simple, browser-native, respects existing border-radius.
Cons: outline doesn't always animate smoothly; can clip outside parent on tightly-nested layouts.

Option 2 — **Box-shadow ring**:
```css
:focus-visible {
  outline: none;
  box-shadow: 0 0 0 3px rgba(44, 55, 119, 0.40);   /* Night at 40% */
}
```
Pros: softer; layers nicely; works on rounded elements without clipping; can be selectively overridden by `box-shadow:none, outline:auto` per element if needed.
Cons: replaces any existing box-shadow unless we compose; requires composing on components that already have shadows.

My recommendation: **Option 2** for consistency with the soft brand aesthetic, applied via a `:focus-visible` rule in `globals.css` and selectively layered on shadowed components.

### B) Brand-color contrast handling

Three colors are failing:
- **Dusk `#BBABF4`** — used in 100+ spots, but mostly as **backgrounds** (lavender panels, accents, modals). Dark text on Dusk: `#130426` on `#BBABF4` = 7.0 : 1 ✅, which is fine. The problem is when Dusk is used as **text** on a cream background (rare, but needs to be verified).
- **Sunset `#DB5835`** — borderline (4.00 : 1). Passes for large text (3:1) which covers headings ≥24px or 18px+bold. Below that, fails.
- **Sunrise `#F29836`** — fails everywhere it carries text.

Per the brief I'm not changing hex values. Three options to apply where these colors carry critical text:

1. **Swap text color to Night / Midnight** when the brand color is purely decorative for the affected element — keeps brand identity in adjacent elements but moves the actual reading text to an accessible value.
2. **Darken the background** behind the brand-color text — e.g., put Sunset text on a darker cream variant.
3. **Use the brand color only for headings ≥24px** (large-text threshold of 3:1 is comfortable for Sunset, still too low for Sunrise/Dusk).

Need a sweep of the 230 occurrences to bucket each into "text role" vs "decorative role" before I can propose specific fixes. Want me to do that sweep next?

### C) `media-has-caption` on voice notes

The audio elements in `VoiceNoteCard`, `VoiceNotePlayback`, `VoiceNoteRecorder` already have an associated **transcript** rendered alongside the audio. The lint rule wants a `<track kind="captions">`. Options:

1. **Disable the rule per-occurrence** with a lint-disable comment + `aria-describedby` pointing to the transcript element. Justification documented inline.
2. **Generate a WebVTT track file** at transcription time and attach it to the audio element. Cleaner, more standards-compliant; more engineering effort.

Option 1 is pragmatic for this audit pass; Option 2 is a roadmap item. Your call.

### D) Runtime audit infrastructure (axe / Lighthouse)

Three viable approaches for the route-level audit:

1. **Manual spot-checks via Chrome DevTools Lighthouse** — fastest to start, free, no infra. You run them yourself per route. I document the findings.
2. **`@axe-core/cli` + Playwright + a session cookie helper** — I write a small script that authenticates as a test user, navigates the route list, and dumps axe JSON to `audits/axe-output/`. Repeatable, but ~half a day of setup.
3. **CI integration** (axe + Lighthouse as a GitHub Action against deployed Vercel preview) — full-fat solution, ~a day of setup, but pays back on every PR.

Recommend (1) for this initial pass, with (2) as a stretch if results are noisy enough to warrant repeated runs.

---

## Recommended next steps (in order)

1. **You pick the focus-style option** (A above). I add the global rule + audit any per-component overrides.
2. **I do the bucket sweep** of brand-color occurrences (decisions for B). Report back with a per-occurrence table; you approve fixes.
3. **I fix the lint-level findings** — 87 items across 12 files. Largest categories are the `<div>`-as-button cases (53 combined) and unassociated labels (19). These are mostly mechanical.
4. **You pick the runtime infra path** (D) and we do the route-by-route runtime audit.
5. **Verification** — re-run lint + runtime suite, log before/after in this report's `## Post-fix verification` section.

---

## Open questions answered

> **1. Are there any routes I haven't listed that should be included in the audit?**

Yes, several:
- `/app/about`, `/app/help` — public-facing content pages, listed in nav but missing from the brief.
- `/app/learn/[area]` leaf pages (`deathcare`, `healthcare`, `legacy`, `personal-admin`, `ritual`, `wills`, `trivia`) — the brief says "Learn index" but the leaf content pages have most of the actual reading content and are where contrast issues are most likely to surface.
- `/app/reflect/scenario-navigator` — interactive multi-step activity not listed.
- `/app/onboarding/legacy-contact` — onboarding form with significant interaction.
- `/app/plan/all`, `/app/plan/export` — Plan subpages.
- `/app/entries/[id]` and its `/export` — view + export of a saved entry.
- `/auth/signup/payment`, `/auth/signup/success`, `/auth/signup/cancel` — Stripe-adjacent flow.
- `/privacy`, `/terms` — long-form content pages.

I'd add these to the route list for axe/Lighthouse runs once we agree on infrastructure.

> **2. Are there any routes that are technically hard to audit?**

- **Anything authed** — needs a logged-in session. Solvable with a Playwright runner + test account.
- **`/auth/callback`, `/auth/signout`** — pure server-side redirects, nothing to audit.
- **Stripe payment flow** — embeds external iframes that axe can't reach into; we audit our wrapper page only.
- **Tour overlays** — fire only on first visit per user. Need to clear `nightside.tour.planPage:{uid}` / `nightside.tour.domain:{uid}` localStorage keys to retrigger.
- **Modal overlays** (Notepad, Feedback, Materials browser) — require user interaction to open. A Playwright script needs to trigger them before snapshotting.
- **Voice-note recording UI** — requires microphone permission, hard to audit in headless modes.
- **Legacy Map drag** — relies on pointer events; axe can't simulate.

> **3. Are there any existing accessibility considerations already in the codebase I should know about before adding new patterns?**

Yes:
- `prefers-reduced-motion` is already respected in 7 places (footer back-to-top, both tours, both rankings, legacy map tooltip, scroll helper). Pattern: `window.matchMedia('(prefers-reduced-motion: reduce)').matches` or `@media (prefers-reduced-motion: reduce)` in CSS.
- ARIA usage exists in several components: GlobalNav hamburger (aria-expanded, aria-controls, aria-label), Breadcrumbs (aria-label="Breadcrumb", aria-current="page"), SlidePanel (role="dialog", aria-modal, aria-label), tour modals (role="dialog", aria-live="polite"), AppFooter (aria-label="Footer", aria-hidden on backdrop).
- `data-tour-anchor` attributes have been added to several elements for the tour system — don't conflict with anything.
- No `:focus-visible` styles are defined globally — the codebase has been relying on browser defaults so far.
- The capture documents and activity pages have a `loading` gate that early-returns a blank `<div>` while data loads (recent change for `/app/domains/[domainId]`). The empty-during-load behavior affects how axe sees the route; we should wait for loaded state before snapshotting.

---

_Will update this report after each fix pass._
