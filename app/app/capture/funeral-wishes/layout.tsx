import type { Metadata } from 'next'

export const metadata: Metadata = {
  title: 'Wishes for My Body, Funeral & Ceremony',
}

export default function Layout({ children }: { children: React.ReactNode }) {
  return children
}
